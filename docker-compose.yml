version: "3.9"
services:
  mongo:
    image: mongo:7
    container_name: fastify_mongo
    ports: ["27017:27017"]
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: root
    volumes:
      - mongodata:/data/db
volumes:
  mongodata:
